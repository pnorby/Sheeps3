@model sheeps3.Models.Game
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    Layout = "_Layout";
    ViewBag.Title = "Start Menu";
}

<form method="post" asp-action="BeginNewGame">
    <div class="row">
    <div class="col-lg-2">
        <label for="player1">Dealer / Player 1</label>
        <br/>
        <select name="player1">
            <option></option>
            @foreach (var player in ViewBag.AvailablePlayers)
            {
                <option value="@player.NickName">@player.NickName</option>
            }
        </select>
    </div>
    <div class="col-lg-1">
        <label for="player2">Player 2</label>
        <select name="player2">
            <option></option>
            @foreach (var player in ViewBag.AvailablePlayers)
            {
                <option value="@player.NickName">@player.NickName</option>
            }
        </select>
    </div>
    <div class="col-lg-1">
        <label for="player3">Player 3</label>
        <select name="player3">
            <option></option>
            @foreach (var player in ViewBag.AvailablePlayers)
            {
                <option value="@player.NickName">@player.NickName</option>
            }
        </select>
    </div>
    <div class="col-lg-1">
        <label for="player4">Player 4</label>
        <select name="player4">
            <option></option>
            @foreach (var player in ViewBag.AvailablePlayers)
            {
                <option value="@player.NickName">@player.NickName</option>
            }
        </select>
    </div>
    <div class="col-lg-1">
        <label for="player5">Player 5</label>
        <select name="player5">
            <option></option>
            @foreach (var player in ViewBag.AvailablePlayers)
            {
                <option value="@player.NickName">@player.NickName</option>
            }
        </select>
    </div>
    <div class="col-lg-1">
        <label for="player6">Player 6</label>
        <select name="player6">
            <option></option>
            @foreach (var player in ViewBag.AvailablePlayers)
            {
                <option value="@player.NickName">@player.NickName</option>
            }
        </select>
    </div>
    <div class="col-lg-1">
        <label for="player7">Player 7</label>
        <select name="player7">
            <option></option>
            @foreach (var player in ViewBag.AvailablePlayers)
            {
            <option value="@player.NickName">@player.NickName</option>
            }
        </select>
    </div>
   </div>
    <br />
    <br />
    <div class="col-lg-12" style="text-align:left">
        <button type="submit">Start</button>
    </div>
</form>
